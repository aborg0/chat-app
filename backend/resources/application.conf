db {
  url = "jdbc:postgresql://localhost:5432/chat_app"
  user = "your_db_user"
  password = "your_db_password"
}

http {
  host = "0.0.0.0"
  port = 8080
}

jwt {
  secret = "your_jwt_secret"
  expiration = 3600
}

logging {
  level = "INFO"
}

metrics {
  enabled = true
}

tracing {
  enabled = true
}